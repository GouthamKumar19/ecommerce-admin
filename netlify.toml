[build]
  command = "npm run build"
  publish = "dist"

# Environment variables can be added here
[build.environment]
  NODE_VERSION = "18"

# Handle SPA routing in React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200